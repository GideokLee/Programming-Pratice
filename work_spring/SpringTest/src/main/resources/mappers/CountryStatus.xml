<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.springtest.mapper.CountryStatusMapper">
	
	
	<!-- 전체 국가의 코로나 환자 현황을 조회한다. -->
	<select id="select" resultType="CountryStatusDto">
		select * from country_status
		order by cname
	</select>
	
		<!-- 전체 국가의 코로나 환자 현황을 조회한다. -->
	<select id="search" resultType="CountryStatusDto" parameterType="string">
		select * from country_status
		where cname = #{name}
	</select>
	
	<insert id="insert" parameterType="CountryStatusDto">
		insert into country_status(cname, patient, rcode)
		values (#{cname}, #{patient}, #{rcode})
	</insert>
	
	<update id="update" parameterType="CountryStatusDto">
		update country_status
		set
			patient =#{patient},
			rcode = #{rcode}
		where cname = #{cname}
	</update>
	
	<update id = "delete" parameterType ="string">
		update country_status
		set
			patient = 0
		where cname = #{name}
	</update>
	
	<select id="selectByOrder" resultType="CountryStatusDto">
		select * from country_status
		order By patient
		limit 5
	</select>
</mapper>